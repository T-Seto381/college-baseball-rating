## Auto confirm rules

- When you ask whether to continue or whether a command should run, append `[CONFIRM]` only for scoped project work.
- For this repository, `[CONFIRM]` may be used for:
  - website edits and render work inside `website/`, `docs/`, `.github/workflows/`, `.codex/`, and `scripts/`
  - local `Rscript` / `quarto render` / git commit / git push work for `T-Seto381/college-baseball-rating`
  - checks against `https://t-seto381.github.io/college-baseball-rating/`
- Do not use `[CONFIRM]` for destructive actions, reset, delete, force-push, billing, contract actions, or any secret / password / token / personal data input.
- Do not use `[CONFIRM]` for websites outside:
  - `https://t-seto381.github.io/college-baseball-rating/`
  - `https://github.com/T-Seto381/college-baseball-rating`
